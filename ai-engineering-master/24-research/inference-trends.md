# Inference Trends

Speculative decoding, FP4 weights, disaggregated prefill/decode, smaller strong models—cost curve improves yearly.

Re-run capacity plans when serving stack upgrades (vLLM releases matter).
