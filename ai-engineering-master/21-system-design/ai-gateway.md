# System Design: AI Gateway

Go service: JWT auth, route table, Redis rate limits, OTel, provider adapters, streaming proxy.

Discuss idempotency keys for downstream side effects—not gateway responsibility alone.
