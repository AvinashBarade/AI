# RAG Evaluation (Module 10)

Measure retrieval and generation separately—recall@k on chunk IDs, faithfulness of answers to retrieved text.

Build queries from real user logs (redacted) plus synthetic edge cases.
